# Superpowers plans & specs — implementation status

**Last updated:** 2026-04-30 (Phase 6.5 Harmony module: all 12 tasks merged on `feature/next-gen-modules-plans`; HarmonyModule with 8 sub-effect modes fully wired — params, FxMatrix dispatch, popup UI, ASSIGNABLE, calibration probe)

This index is the authoritative status of every plan and design spec under
`docs/superpowers/`. **Agents: consult this file before following any plan or
design doc.** Each individual plan/spec also carries a status banner at the top,
but if the two disagree, this index wins.

**Status legend**

- **IMPLEMENTED** — merged to master; live in the codebase. The doc is kept for
  historical context. The code is now the source of truth, not the plan.
- **IN PROGRESS** — actively being executed task-by-task.
- **DEFERRED** — approved but intentionally not started yet. Still valid as a
  design reference.
- **SUPERSEDED** — never landed as written; a later plan/architecture replaced
  the approach. Do **not** follow these plans.

---

## Plans (`docs/superpowers/plans/`)

| Plan | Status | Notes |
|------|--------|-------|
| 2026-04-12-spectral-forge.md | IMPLEMENTED | Foundational plan. Single-engine `Pipeline` was later replaced by the modular `FxMatrix`; treat this plan as historical. |
| 2026-04-14-effects-engines.md | SUPERSEDED | Effects (Freeze, PhaseRand, Contrast) originally shipped as a fixed post-compressor stage. They now live as independent `SpectralModule` implementations in the FxMatrix. |
| 2026-04-14-gui-tabs-skeleton.md | SUPERSEDED | The Dynamics/Effects/Harmonic tab bar was removed when the per-slot module UI landed in plan D2. |
| 2026-04-19-modular-matrix.md | IMPLEMENTED | Infrastructure for the 8×8 FxMatrix + Dynamics slot. |
| 2026-04-19-plan-d1-module-foundation.md | IMPLEMENTED | `SpectralModule` trait, `RouteMatrix`, `apply_curve_transform`, module stubs. |
| 2026-04-19-serial-fx-chain.md | SUPERSEDED | Bridge plan to serialise Dynamics → Freeze → PhaseSmear via bool flags. Skipped in favour of the full FxMatrix approach. Do not follow. |
| 2026-04-19-variable-fft.md | IMPLEMENTED | Runtime-selectable FFT size (512 … 16384). |
| 2026-04-20-d2-ux-modules.md | IMPLEMENTED | Module popup, adaptive curve editor, per-slot SC/GainMode/name, matrix routing, T/S virtual rows, M/S DSP. |
| 2026-04-20-functional-module-bugs.md | IMPLEMENTED | Contrast / T/S Split / M/S Split / Freeze / silent-master fixes. |
| 2026-04-21-automation-presets.md | IMPLEMENTED | Generated automatable params (~1341), 1000 ms tooltips, JSON preset system. |
| 2026-04-21-bin-physics-infrastructure.md | SUPERSEDED | Replaced by `2026-04-27-phase-3-bin-physics.md`. Architecture diverged: per-field `MergeRule`, opt-in writer/reader split via `writes_bin_physics`, auto-velocity in FxMatrix. |
| 2026-04-21-sidechain-refactor-plan.md | IMPLEMENTED | Single SC port + per-slot gain/channel selector; peak-hold curve; Freeze default threshold fix. |
| 2026-04-23-ui-parameter-spec.md | IMPLEMENTED | `CurveDisplayConfig`, curvature transform, shared hover routine, UI scaling. |
| 2026-04-24-calibration-audit.md | IMPLEMENTED | All 11 tasks merged: per-module ProbeSnapshot + calibration round-trip tests, Dynamics/Freeze DSP clamp fixes, 10 kHz curve cutoff fix, hover-tooltip flip, Freeze row layout fix, UI spec addenda §2.3/§3.4/§4.4. |
| 2026-04-24-ui-spec-cleanup.md | IMPLEMENTED | Closed the 2026-04-24 spec-deviation review. |
| 2026-04-27-phase-1-foundation-infra.md | IMPLEMENTED | All 8 PRs landed. ModuleContext borrowed, ModuleSpec hints (`wants_sidechain`, `panel_widget`, `heavy_cpu_for_mode`), `enable_heavy_modules` toggle, Reset-to-default button + `clear_state` plumbing, modulation-ring scaffold (toggles disabled until BPM sync). Unblocks every Phase 2+ sub-plan. |
| 2026-04-27-phase-2a-matrix-amp-nodes.md | IMPLEMENTED | All 12 tasks merged: AmpMode/AmpCellParams + per-mode kernels (Vactrol/Schmitt/Slew/Stiction), RouteMatrix amp_mode + amp_params with serde-default, lazy-allocated FxMatrix amp_state, per-cell apply at all three accumulation sites in process_hop, theme dot constants + popup + matrix-cell indicator/right-click, calibration probe behind `feature = "probe"`, finite/bounded e2e test. |
| 2026-04-27-phase-2b-future.md | IMPLEMENTED | All 9 tasks merged: FutureModule + PrintThrough/PreEcho kernels, per-channel ring (`MAX_ECHO_FRAMES = 64`), two-pass spread with phase-preserving `spread_scratch`, PreEcho feedback capped at 0.4 for closed-loop stability, per-slot `slot_future_mode` persistence + UI mode picker, ASSIGNABLE entry, calibration probes for both modes. Lookahead Duck + Crystal Ball deferred per audit. |
| 2026-04-27-phase-2c-punch.md | IMPLEMENTED | All 10 tasks merged: PunchModule + Direct/Inverse modes sharing one carve kernel (Inverse uses dedicated `peak_scratch`, never clobbers the smoothing follower), greedy local-max peak detector with `min_dist` veto + 256-slot on-stack scratch, 5 ms attack / HEAL-controlled release follower with τ=150 ms nominal + neighbour amp-fill, sub-bin pitch-fill via slew-limited `drift_accum` clamped to ±0.5 bins (phase rotation `Δφ = (π/2)·d` at OVERLAP=4), per-slot `slot_punch_mode` persistence (mirrors `slot_future_mode`), unified Mode-row UI (Future + Punch in a single visible-when-applicable row), calibration probes mode-agnostic. First module to default `wants_sidechain: true`. Self-Punch deferred per audit. |
| 2026-04-27-phase-2d-rhythm.md | IMPLEMENTED | All 11 tasks merged: Pipeline now populates `ctx.bpm` + `ctx.beat_position` from `Transport`. RhythmModule with Euclidean (Bjorklund into `[bool; 32]` on-stack scratch, DIVISION quantised to {1,2,4,8,16,32} steps), Arpeggiator (greedy peak picker on step crossings, 8×8 `ArpGrid` voice gating, per-voice envelope with `attack_step_frac` ramps), Phase Reset / Laser (per-step transient phase overwrite blending toward TARGET_PHASE; DC + Nyquist hard-skipped to keep realfft inverse well-defined). Per-slot mode + grid persistence via `Arc<Mutex<[T; 9]>>` (mirrors `slot_future_mode` / `slot_punch_mode`, NOT the plan's per-slot `[Mutex<T>; MAX_SLOTS]`); audio-thread dispatch is `try_lock`-once via `FxMatrix::set_rhythm_modes_and_grids`. `PanelWidgetFn` widened to `fn(&mut Ui, &SpectralForgeParams, slot)` so the 8×8 step-grid widget at `editor/rhythm_panel.rs` can read params; cell colours come from `module_spec(...).color_lit/.color_dim`, strokes via `th::scaled_stroke(th::STROKE_HAIRLINE, scale)` for UI scaling. UI Mode-row extended to a third module type using themed buttons (NOT the plan's `ComboBox`). 9 calibration probes (3 modes × {AMOUNT default/max, MIX max}) via a `run_rhythm_case` helper that sets `ctx.bpm = 120.0`, `beat_position = 0.0` (step_idx=0, where Bjorklund E(4,8) and E(8,8) both pulse). Bin Swing deferred (Spectral Delay infra); NoteIn-trigger deferred (Phase 6.3 MIDI). |
| 2026-04-27-phase-2e-geometry-light.md | IMPLEMENTED | All 10 tasks merged: GeometryModule with Chladni Plate Nodes (two-pass eigenmode kernel, 5%/hop AMOUNT cap, ~5% energy conservation) and Helmholtz Traps (8 fixed log-spaced traps + soft notch + overflow → 2nd-harmonic overtone re-injection). Center re-injection deliberately omitted (lies inside absorption band → leaky notch). Two safety nets in `apply_helmholtz` to bound runaway: `fill_level.min(2.0 * trigger)` and overtone-bin magnitude clamp at 1000.0 — added by Task 8 after the e2e test exposed orphan-overtone accumulation (e.g. bin 98 = 2×trap-4-center, outside every trap's band → reaches 1e6 norm by ~hop 106 under sustained excitation). Per-slot mode persistence via `Arc<Mutex<[GeometryMode; 9]>>` (matches `slot_future_mode` family, NOT the plan's `[Arc<Mutex<T>>; MAX_SLOTS]`); FxMatrix dispatch via `set_geometry_modes` + `set_geometry_mode` trait method. UI Mode-row extended to a fourth module type using themed Chladni/Helmholtz buttons (NOT the plan's separate `geometry_popup.rs`). Calibration uses shared `ProbeSnapshot { amount_pct, mix_pct }` (NOT the plan's bespoke `GeometryProbe`); 6 probes (2 modes × {AMOUNT default/max, MIX max}). 11 dedicated tests in `tests/geometry.rs`. Wavefield + Persistent Homology defer to Phase 7 (need SIMD wave kernel + History Buffer). |
| 2026-04-27-phase-2f-modulate-light.md | IMPLEMENTED | All 11 tasks merged: ModulateModule + 5 light kernels (Phase Phaser with hop_count rotation + AmpGate, Bin Swapper with scratch-buffer snapshot then offset blend, RM/FM Matrix blending sidechain-magnitude RM vs phase-rotation FM, Diode RM with amplitude-gated carrier leak, Ground Loop with 16-frame RMS history ring + 50/60Hz mains-hum injection). Per-channel state: `hop_count[2]`, `swap_scratch[2]: Vec<Complex<f32>>`, `rms_history[2][16]`. Per-slot mode persistence via `Arc<Mutex<[ModulateMode; 9]>>` (matches `slot_geometry_mode` family); FxMatrix dispatch via `set_modulate_modes`. UI Mode-row extended to a fifth module type using themed buttons (Phase Phaser / Bin Swapper / RM/FM / Diode RM / Ground Loop). Calibration uses shared `ProbeSnapshot { amount_pct, mix_pct }`; 15 probes (5 modes × {AMOUNT default/max, MIX max}) plus 16 dedicated kernel tests in `tests/modulate.rs`. `wants_sidechain: true` so RM/FM and Diode RM auto-route Sc(0) on assignment. **Retrofitted** with Gravity Phaser + PLL Tear in `2026-04-27-phase-5b4-modulate-retrofit.md`. FM Network + Slew Lag deferred to Phase 6. |
| 2026-04-27-phase-5b4-modulate-retrofit.md | IMPLEMENTED | All 13 tasks merged on `feature/next-gen-modules-plans`: Modulate gains two physics-aware modes — **Gravity Phaser** (force-driven per-bin phase rotation reading/writing `BinPhysics.phase_momentum`, with per-slot `mod_gp_repel` and `mod_gp_sc_positioned` toggles) and **PLL Tear** (per-bin 2nd-order PI PLL bank consuming `ctx.unwrapped_phase` when provided, falling back to local unwrap; lock-loss tear emission with hysteresis: `PLL_TEAR_THRESHOLD = π/2`, `PLL_RELOCK_THRESHOLD = π/8`, `PLL_RELOCK_HOPS = 4`, `PLL_MIN_BIN = 16`). Shared `physics_helpers.rs` gained `pll_bank_step` + `wrap_phase` for Phase 5b.4 and v2 reuse. `ModuleSpec.writes_bin_physics` flipped to `true` for Modulate. `ProbeSnapshot` extended with 4 retrofit fields (`mod_pll_lock_pct`, `mod_gp_node_count`, `mod_gp_repel`, `mod_gp_sc_positioned`). UI Mode-row extends to seven Modulate modes (5 light + GravityPhaser + PllTear). Tests in `tests/modulate.rs`, `tests/module_trait.rs`, `tests/calibration_roundtrip.rs` (probe-feature-gated). FM Network + Slew Lag remain deferred to Phase 6+. |
| 2026-04-27-phase-3-bin-physics.md | IMPLEMENTED | All 8 tasks merged: `BinPhysics` carrier struct with per-field `MergeRule` (WeightedAvg / Max / HeavierWins, gated on weight>0); `writes_bin_physics: bool` opt-in on `ModuleSpec`; `physics: Option<&mut BinPhysics>` arg on `SpectralModule::process` + `ctx.bin_physics: Option<&BinPhysics>` for readers; `FxMatrix` plumbing — per-slot `slot_phys`, `mix_phys` workspace, SoA `prev_mags` for auto-velocity, writer-first `phys_order` topology with debug-assert guard against the still-pending stale-audio defect (writers must be slot 0 today; Phase 5 will lift); writer/reader dispatch split at both slot and Master sites; integration test in `tests/bin_physics_pipeline.rs` (probe-feature-gated) proving writer→reader flow via `test_force_writer` helper; `ProbeSnapshot` extended with `bp_*` field shapes for Phase 5 round-trip wiring. Supersedes `2026-04-21-bin-physics-infrastructure.md`. |
| 2026-04-27-phase-5c-full-circuit.md | IMPLEMENTED | All 15 tasks landed on `feature/next-gen-modules-plans` (commits `0a92315` … `f434088`): Circuit retrofit from 3 → 10 modes adding 7 new BinPhysics-aware sub-effects — **Vactrol** (cascaded 1-pole, reads `BinPhysics::flux`), **TransformerSaturation** (tanh + magnitude one-pole + SPREAD 3-tap leak, reads/writes flux, `heavy_cpu` flagged), **PowerSag** (per-channel energy-driven envelope, reads `BinPhysics::temperature`, attack τ≈50 ms / release controlled by RELEASE curve), **ComponentDrift** (slow per-bin xorshift LFSR drift with τ ∈ [1, 9] s, reads/writes temperature; gate is per-bin `temp_gate = THRESHOLD`), **PcbCrosstalk** (`spread_3tap` symmetric stencil weighted by SPREAD), **SlewDistortion** (magnitude rate-limit gated by THRESHOLD + xorshift32 phase scramble, writes `BinPhysics::slew`), **BiasFuzz** (DC offset envelope `bias_lp` converging to in_mag at α=0.05, asymmetric `tanh_levien_poly` clip with top rail = THRESHOLD, reads/writes `BinPhysics::bias`). Bumps `num_curves` from 4 to 5: inserts SPREAD at index 2; the curve layout is `[AMOUNT, THRESHOLD, SPREAD, RELEASE, MIX]`. Migrates the three v1 kernels (BBD/Schmitt/Crossover) to the new indices in-place. Flips `ModuleSpec.writes_bin_physics` to `true`. New file `src/dsp/circuit_kernels.rs` ships shared scalar SIMD-friendly primitives (`lp_step`, `tanh_levien_poly`, `spread_3tap`, `SimdRng`) reused across modes 4-10. `CircuitModule` state grows with per-mode fields (vactrol two-stage caps, xfmr magnitude smoother + workspace, sag_envelope per channel, drift LFSR seed array, prev_mag for slew, bias_lp/bias_out for Bias Fuzz). Mode-picker UI moved from inline buttons (Phase 2g pattern) to a dedicated `editor/circuit_popup.rs` with 10 entries + `(heavy)` suffix on TransformerSaturation; matches Life/Past/Kinetics popup pattern. Calibration coverage: `tests/calibration_roundtrip.rs` extended with 30 probe round-trip tests (10 modes × {AMOUNT default/max, MIX max}) plus mode-specific physics-probe accumulator test (PowerSag drives `sag_envelope > 0`, ComponentDrift `drift_env_avg.abs() > 1e-5` over 200 hops). Multi-hop dual-channel finite/bounded contract test in `tests/circuit.rs::circuit_finite_bounded_all_modes_dual_channel` extended from 3 → 10 modes (200 hops × 2 channels per mode, `physics: None` exercises the dry-fallback path inside writer modules). BinPhysics integration tests in `tests/bin_physics_pipeline.rs` cover Vactrol flux read (A/B with `flux=4.0` vs `flux=0.0`), Transformer flux roundtrip, Bias Fuzz bias roundtrip (analytical steady-state ≈ 2050 with constant input). Test binaries `calibration_roundtrip`, `bin_physics_pipeline`, `calibration`, `circuit` are all `required-features = ["probe"]`-gated. |
| 2026-04-27-phase-2g-circuit-light.md | IMPLEMENTED | All 10 tasks merged: CircuitModule + 3 light kernels (BBD Bins with 4-stage `bbd_mag[2][4]` magnitude pipeline + per-channel xorshift32 dither + RELEASE-controlled LP smoothing, Spectral Schmitt with branch-free per-bin hysteresis latch `schmitt_latched[2][num_bins]: u8` + `OFF` bins attenuated by AMOUNT × 0.5, Crossover Distortion stateless C¹-smooth deadzone `(mag - dz)² / mag` with `dz_width = AMOUNT × 0.1`). BBD emits real-positive `Complex::new(out_mag, 0.0)` when input is silent (no carrier phase to preserve) so propagation hops still work with zero input. `wants_sidechain: false` — Circuit v1 is sidechain-free. Default mode = CrossoverDistortion (cheapest, passthrough-safe at AMOUNT=0/MIX=0). Per-slot mode persistence via `Arc<Mutex<[CircuitMode; 9]>>` (matches `slot_modulate_mode` family); FxMatrix dispatch via `set_circuit_modes` + `try_lock`-once-per-block in `Pipeline::process`. UI Mode-row extended to a sixth module type using themed Crossover/Schmitt/BBD buttons (NOT the plan's separate `circuit_popup.rs`). Calibration uses shared `ProbeSnapshot { amount_pct, mix_pct }` with uniform `g.clamp(0,2) * 50.0` mapping; 9 probes (3 modes × {AMOUNT default/max, MIX max}) plus 9 dedicated tests in `tests/circuit.rs` including 200-hop × 2-channel × 3-mode finite/bounded regression guard with per-hop input snapshot reset. Defers Vactrol / Transformer Saturation / Power Sag / Component Drift / PCB Crosstalk / Slew Distortion / Bias Fuzz / Resonant Feedback to Phase 5c (BinPhysics-aware modes). |
| 2026-04-27-phase-5a-life.md | IMPLEMENTED | All 18 tasks merged: LifeModule with 10 sub-effect modes (Viscosity FTCS diffusion, SurfaceTension coalescence, Crystallization phase-lattice with `BinPhysics.crystallization` write, Archimedes volume-conserving ducking, NonNewtonian velocity-driven transient limiter, Stiction static/kinetic friction, Yield fabric tearing with phase scramble, Capillary three-pass upward harmonic wicking, Sandpaper granular phase friction sparks, Brownian temperature-driven random walk). 5 curves (AMOUNT, THRESHOLD, SPEED, REACH, MIX). Per-channel state: `wick_envelope`, `wick_carry`, `tear_state`, `sustain_envelope`, `is_moving`, plus `scratch_power`/`scratch_mag` two-pass scratch and `rng_state: u32` xorshift for Brownian/Yield/Sandpaper. Per-slot mode persistence via `Arc<Mutex<[LifeMode; 9]>>` (matches `slot_circuit_mode` family); FxMatrix dispatch via `set_life_modes` + `set_life_mode` trait method. Dedicated `editor/life_popup.rs` (egui temp-data pattern) for the 10-mode picker — single Mode-button opens popup rather than inline buttons (the 5 prior mode-row modules used inline buttons; 10 modes are too many). Calibration uses `LifeProbe { active_mode, recent_sustain_max, recent_tear_count }` (probe-feature-gated). Energy-conservation invariant test (`tests/life_conservation.rs`) covers transport modes (Viscosity 5%, SurfaceTension 10%, Capillary 15%, Archimedes monotone-non-creating). Multi-hop dual-channel finite/bounded test in `tests/module_trait.rs::life_all_modes_finite_and_bounded` (200 hops × 2 channels × 10 modes under stress curves). Defers Crystallization↔Freeze cooperation, multi-mode-per-slot stacking, cepstral envelope baseline (see Follow-ups in `specs/2026-04-21-life-module.md`). |
| 2026-04-27-phase-4-plpv-phase.md | IMPLEMENTED | All 10 tasks merged: per-bin Laroche-Dolson phase unwrap (`src/dsp/plpv.rs::unwrap_phase` + `principal_arg`), low-energy bin phase damping (smoothstep blend across ±6 dB band centred on `plpv_phase_noise_floor_db`), 4-neighbour peak detection + Voronoi skirt assignment (`detect_peaks`, `assign_voronoi_skirts`), Pipeline integration with `Cell<f32>`-based interior-mutability exposure of `unwrapped_phase` so `ModuleContext` stays `Copy`. Per-module integrations: Dynamics (peak-locked GR broadcast across skirts via `bp_threshold` shared per-peak), PhaseSmear (random phase summed into `unwrapped[k]` instead of complex-space mix), Freeze (`frozen_unwrapped` advances at canonical PV rate `2π·k·hop/N` per hop), MidSide (per-peak broadcast for both mid balance and side balance/expansion/decorrelation rotation). Per-module setter convention: `set_plpv_<module>_enabled` with default no-op trait method; only the matching module overrides. Four `plpv_<module>_enable: BoolParam` (default true) for A/B comparison, walked through `FxMatrix::set_plpv_<module>_enable` once per block. Audio-render regression suite (`tests/plpv_audio_render.rs`): sine-sweep centroid stability (1.65× margin), drum-loop within-skirt GR broadcast (∞× margin, structural), frozen-chord inter-frame phase coherence with canonical advance (25.6× margin). Calibration round-trip tests in `tests/plpv_calibration.rs` (~9 tests, probe-feature-gated). v2 adaptive per-frame coherence policy explicitly deferred. |
| 2026-04-27-phase-5b1-history-buffer.md | IMPLEMENTED | All 8 tasks merged: `HistoryBufferDepthChoice` enum param (1/2/4/8/16 s, default 4 s) with hop-aware `max_frames(sr, fft)` helper; `HistoryBuffer` struct (per-channel `Vec<Vec<Vec<Complex<f32>>>>` ring with shared `write_pos`, `frames_used`, `read_frame(channel, age) -> Option<&[Complex<f32>]>` API + `read_fractional` lerp for sub-hop ages); lazy summary stats (`decay_estimate` via log-mag linear regression with `MAG_FLOOR`/`SLOPE_DEADBAND`/`DECAY_CLAMP` named consts, `rms_envelope`, `if_stability = 1/(1+Var(Δφ))` with TAU phase wrap) cached behind `RefCell<SummaryCache>` and invalidated by `clear_summary_cache()` at the top of every block + `advance_after_all_channels_written()`; ANALYSIS_WINDOW=32 frames (~340 ms at default fft 2048 hop 512 / 48 kHz); `ctx.history: Option<&HistoryBuffer>` borrowed read-only across the StftHelper closure (modules see strictly *prior-block* frames — split-borrow pattern via `pending_hop_frames` scratch pad drained AFTER the closure returns); `Pipeline::new`/`reset` extended with `history_depth_seconds` parameter (`((depth × sr) / hop).ceil()` capacity, realloc on depth/capacity/channel change); `lib.rs` reads `params.history_depth.value().seconds()` at both call sites; `clear_state()` extended to zero pending pad + `history.reset()` (allocation-free); test-only `Pipeline::history_probe(channel)` + `HistoryProbe { frames_used, capacity, summary_*_max }` gated on `cfg(any(test, feature = "probe"))`; integration smoke test (`tests/history_buffer_pipeline.rs`, 200-hop synthetic load × 2 channels × 1025 bins) and calibration probe test (`tests/calibration.rs`). v1 limitation: writes one hop per block (last hop captured inside StftHelper); ANALYSIS_WINDOW averaging compensates. No audible feature ships in this phase — readers (Past, Kinetics) land in Phases 5b.2/5b.3. |
| 2026-04-27-phase-5b3-kinetics.md | IMPLEMENTED | All 17 tasks merged: first BinPhysics writer/reader hybrid module. `KineticsModule` with 8 sub-effect modes (Hooke neighbour-spring restoring force with sympathetic-harmonic coupling, GravityWell with WellSource ∈ {Static / Sidechain / MIDI} where MIDI degrades to no-op until Phase 6 plumbs `ctx.midi_notes`, InertialMass with MassSource ∈ {Static / Sidechain} writing per-bin mass into `BinPhysics.mass`, OrbitalPhase satellite-rotation around detected peaks, Ferromagnetism phase alignment to nearest peak, ThermalExpansion heat→phase-detune accumulator, TuningFork peak-driven neighbour modulation with `tuning_forks: SmallVec<[(usize, f32); 16]>` gap mode, Diamagnet 1/d energy-conserving spectral carving). 5 curves (STRENGTH, MASS, REACH, DAMPING, MIX). Per-channel state: `displacement`, `velocity`, `temperature_local`, `mag_prev`, `prev_phase`, `prev_kepe`, `kepe_rose_last_hop`, `smoothed_curves[5]`, `tuning_forks` SmallVec, plus `max_dry_smoothed[2]` slow-attack envelope (ALPHA = 0.01, τ ≈ 100 hops) primed to `cur_max` on the first post-reset hop — feeds the displacement-cap reference in `apply_hooke` and `apply_gravity_well` so an upstream slot's amplification cannot raise the cap and feed a runaway loop. Velocity-Verlet integrator + CFL clamp + 1-pole curve smoothing (`tau = 4·dt`) + viscous-damping floor + energy-rise hysteresis (two-consecutive-doubling guard) live in shared `src/dsp/physics_helpers.rs` so Phase 5b.4 Modulate retrofit and v2 expansions reuse them. Per-mode `heavy_cpu` flag (per-mode, not per-module): TuningFork + Diamagnet flagged heavy. Per-slot mode + sub-source persistence via `Arc<Mutex<[KineticsMode; 9]>>` + `Arc<Mutex<[WellSource; 9]>>` + `Arc<Mutex<[MassSource; 9]>>` (matches `slot_past_mode` family); FxMatrix dispatch via `set_kinetics_modes` + `set_kinetics_well_sources` + `set_kinetics_mass_sources` (all `try_lock`-once with no-op fallback). Dedicated `editor/kinetics_popup.rs` (egui temp-data pattern, mirrors `past_popup.rs`) — single Mode-button opens popup; popup stays open after mode-click so user can also pick the WellSource/MassSource sub-source for GravityWell/InertialMass in one trip; popup re-reads `mode_now` after the mode loop to avoid one-frame flicker on sub-picker visibility transitions. Calibration uses shared `ProbeSnapshot` extended with 6 Kinetics fields (`kinetics_strength`, `kinetics_mass`, `kinetics_displacement`, `kinetics_velocity`, `kinetics_active_mode_idx`, `kinetics_well_count`) populated as a struct-literal at the end of `process()`; `last_probe()` is a one-line override; calibration test in `tests/calibration_roundtrip.rs::kinetics_calibration_probes_round_trip`. Integration test `tests/kinetics_integration.rs` covers writer→reader BinPhysics path (InertialMass-Static, 30-hop settle ≈ 6.6·τ to drive the smoother to within e⁻⁶·⁶ of target) and chained-slot stability (GravityWell→Hooke, 50 hops, |bin| < 100). Defers to v2: 8× substep oversampling for Springs slot (lifts CFL ceiling ~50 Hz → ~400 Hz), symplectic-Euler "mini orbit" with per-satellite (φ, ω) (v1 ships linear phase rotation), Kepler-style elliptical orbits (skip entirely, ~30× cost indistinguishable through iFFT), Dynamics-reading-BinPhysics retrofit (Dynamics is feature-frozen), global "Ferromagnetic Snap" Modulation Ring option, MIDI-tracked GravityWell sources beyond no-op stub, CSR sympathetic-spring layout (per-bin fixed `[u16; 8]` is fine until users hit the cap). |
| 2026-04-27-phase-6-pitch-harmony-umbrella.md | IMPLEMENTED | All gating sub-plans (6.1 IF, 6.2 chromagram+groups, 6.3 MIDI, 6.4 cepstrum, 6.5 Harmony) merged on `feature/next-gen-modules-plans`. Phase 6.6 (FM Network/Arp NoteIn) and 6.7 (Modulation Ring UI) remain PLANNED. |
| 2026-04-27-phase-6.5-harmony-module.md | IMPLEMENTED | All 12 tasks merged on `feature/next-gen-modules-plans`. HarmonyModule with 8 sub-effect modes (Chordification 24-template, Undertone IF-driven sub-harmonics + ground-loop hum, Companding per-class harmonic-group attenuation, Formant Rotation cepstrum-preserving envelope rotation, Lifter cepstrum-domain envelope/pitch shaping, Inharmonic Stiffness/Bessel/Prime detuning, Harmonic Generator from peaks, Shuffler bin-swap with SPREAD reach). 6 curves (AMOUNT, THRESHOLD, STABILITY, SPREAD, COEFFICIENT, MIX). Per-slot HarmonyMode + HarmonyInharmonicSubmode persisted via Arc<Mutex<[T; 9]>> (mirrors kinetics family); FxMatrix dispatch via set_harmony_modes + set_harmony_inharmonic_submodes. Dedicated editor/harmony_popup.rs (mode + Inharmonic sub-mode picker — popup stays open for sub-mode pick, mirrors kinetics_popup). HARM2 ModuleSpec opts into needs_instantaneous_freq + needs_cepstrum + needs_chromagram + needs_harmonic_groups (single Harmony slot activates all four lazy infra paths). Calibration uses shared ProbeSnapshot (amount_pct, mix_pct). FM Replicator + Persistent Homology defer to Phase 7; sustained-only harmonic detection per umbrella scope. |
| 2026-04-27-phase-6.2-chromagram-harmonic-groups.md | IMPLEMENTED | All 5 tasks merged on `feature/next-gen-modules-plans` (commits `d44ce40` … `b349697`): two complementary per-hop pitch-aware analysis kernels. `src/dsp/chromagram.rs::compute_chromagram(bins, ifreq, sr, fft, &mut [f32; 12])` runs IF-refined HPCP (uses Phase 6.1's IF when available, falls back to bin-center frequencies); ~6 FLOPs + 1 log2 per qualifying bin in [80 Hz, 5 kHz]; output normalised to unit sum on non-silent input, all-zero on silent. `src/dsp/harmonic_groups.rs` ships `HarmonicGroup { fundamental_hz, harmonic_count, harmonic_bins: [u16; 16], total_magnitude }` + `HarmonicGroupBuf::detect(...)` with top-`MAX_PEAK_CANDIDATES = 32` magnitude scan, insertion-sort descending, greedy fundamental-first sweep with ±25¢ tolerance (`tol_ratio = 2^(25/1200)`), per-bin `claimed: Vec<bool>` (sized at `MAX_NUM_BINS`, allocated once in `new()`) preventing double-counting across groups, and 2-harmonic-minimum reject for stray peaks. Up to `MAX_GROUPS = 4` voices per hop. `ModuleSpec` gains `needs_chromagram: bool` + `needs_harmonic_groups: bool` (default false in all 19 spec literals). `ModuleContext` gains `harmonic_groups: Option<&'block [HarmonicGroup]>` (chromagram field already existed from Phase 1). `Pipeline` carries per-channel `chromagram_buf: Vec<[f32; 12]>` + `harmonic_groups: Vec<HarmonicGroupBuf>`, allocated in `new()`, `resize`d in `reset()`. Per-block `any_needs_chromagram` / `any_needs_groups` scans use `slot_types_snap` (mirrors Phase 6.1 + 6.4 pattern, no extra lock). `any_needs_groups` force-enables `any_needs_if` (groups consume IF) — `any_needs_if` flipped to `let mut`. Per-hop compute lives inside the StftHelper closure after the IF + cepstrum blocks; results wired via `hop_ctx.{chromagram, harmonic_groups}` through local-rebound `chromagram_buf_ref` / `harmonic_groups_ref`. Cost when no slot opts in: literally two bool checks per block — both kernels entirely skipped. Tests: 5 in `tests/chromagram.rs` (A440 → pitch class 9, unit-sum normalisation, silent → all-zero, subsonic <80 Hz rejection, ModuleSpec exhaustive non-chromagram + non-groups guard) + 4 in `tests/harmonic_groups.rs` (220 Hz harmonic series detection, silent → empty, MAX_GROUPS cap under 10 fundamentals stress, HARMONIC_TOLERANCE_CENTS sanity bounds). Defers to Phase 6.5: chord-template matching (60-template cosine matcher), bigram smoothing of chord predictions; defers to Phase 7: transient-inclusion harmonic-group attribution; deferred entirely: neural pitch models. Unblocks Phase 6.5 Harmony Chordification + sub-effects that consume per-hop pitch-class profile or fundamental-tracked groups. |
| 2026-04-27-phase-6.4-cepstrum.md | IMPLEMENTED | All 6 tasks merged on `feature/next-gen-modules-plans` (commits `10c0a5c` … `b662125`): lazy per-hop cepstrum analysis utility for the upcoming Harmony Lifter (Phase 6.5). New `src/dsp/cepstrum.rs` ships `CepstrumBuf` wrapping a `realfft::ComplexToReal` planner — `compute_from_bins(&[Complex<f32>])` runs `log_mag[k] = 0.5 * ln(|bins[k]|² + ε)` (ε = 1e-10 magnitude-squared, per `research/02-pitch-and-cepstral.md`) into the inverse real-FFT, producing real `cepstrum[n]` of length `fft_size`. Buffers sized at `MAX_NUM_BINS` / `MAX_FFT_SIZE`; `resize(fft_size)` rebuilds the planner off the audio thread (called from `Pipeline::reset` when FFT size changes). `ModuleSpec` gains `needs_cepstrum: bool` (default false) on all 19 spec literals; `ModuleContext` gains `cepstrum_buf: Option<&'block [f32]>`. `Pipeline` carries `cepstrum_buf: Vec<CepstrumBuf>` (one per channel) and computes per-block `any_needs_cepstrum` from `slot_types_snap` (no extra lock). Local-rebind pattern (`cepstrum_buf_ref = &mut self.cepstrum_buf`) before the StftHelper closure mirrors the IF infrastructure pattern. Per-hop compute happens after the IF block but before `fx_matrix.process_hop(...)`; result is wired via `hop_ctx.cepstrum_buf = Some(cepstrum_buf_ref[ch].quefrency())`. Cost when no slot opts in: literally one bool check per block — extra inverse real-FFT entirely skipped. Tests: 5 in `tests/cepstrum.rs` (kernel pure-tone finite + silence finite + reusable across calls + ModuleSpec exhaustive non-cepstrum guard + Pipeline-build smoke test). Silence cross-fade deferred to 6.5 — kernel always returns a finite buffer thanks to the ε clamp. Unblocks Phase 6.5 Harmony Lifter sub-effect. |
| 2026-04-27-phase-6.3-midi-input.md | IMPLEMENTED | All 7 tasks merged on `feature/next-gen-modules-plans` (commits `5ad4808` … `784ebd7`): nih-plug MIDI port wired via `Plugin::MIDI_INPUT = MidiConfig::Basic`. `src/dsp/midi.rs` ships pure helpers `apply_note_on` / `apply_note_off` / `clear_midi_state` over `[bool; 128]` held + `[bool; 12]` pitch-class arrays (note-off recomputes the class bit so an octave-held release doesn't clear C while C5 still sounds; out-of-range notes silently ignored; allocation-free). `Pipeline` carries `held_notes: [bool; 128]` + `held_pitch_classes: [bool; 12]` (no heap; `clear_midi_state` called from both `reset()` and the audio-thread `clear_state()` so the GUI hard-reset never leaves stale notes). Public mutators `Pipeline::note_on(u8)` / `note_off(u8)` + read-only `held_notes()` for tests. `SpectralForge::process()` drains `ctx.next_event()` into the mutators **before** `pipeline.process(...)`, matching `NoteEvent::NoteOn { note, .. }` / `NoteOff { note, .. }` (sample-accurate per-hop note timing deferred to v2; v1 semantics = block-start keyboard state, sufficient for held-note triggering and chord-class consumption). `ModuleContext` gains `held_pitch_classes: Option<&'block [bool; 12]>` paired with the existing `midi_notes: Option<&'block [bool; 128]>`; both wired from Pipeline locals (`held_notes_ref`, `held_pcs_ref`) inside the hop closure. 8 tests in `tests/midi_plumbing.rs` (helper round-trip × 6 + Pipeline `note_on/off` round-trip + Pipeline `reset` clears MIDI). Unblocks Phase 6.5 Harmony Chordification, Phase 6.6 Arpeggiator NoteIn, Phase 6.7 Modulation Ring Legato/Sync, plus Kinetics' GravityWell MIDI sub-source (was no-op since 5b3). |
| 2026-04-27-phase-6.1-instantaneous-freq.md | IMPLEMENTED | All 9 tasks merged on `feature/next-gen-modules-plans` (commits `6f37c4a` … `b2e06b2`): per-bin instantaneous frequency infrastructure. New helper `src/dsp/instantaneous_freq.rs` with `principal_argument` (re-export of `plpv::principal_arg`) and `compute_instantaneous_freq(prev, curr, out, sr, hop, fft)` using the standard phase-vocoder deviation formula `IF[k] = k·bin_freq_hz + principal_arg(curr-prev-2π·k·hop/fft) · sr/(hop·2π)`, DC bin pinned to 0 Hz. `ModuleSpec` gains `needs_instantaneous_freq: bool` (default false) — Phase 6+ opt-in. `Pipeline` gains per-channel `if_buffer` and `if_prev_phase: Vec<Vec<f32>>` (sized at `MAX_NUM_BINS`, zeroed by both `clear_state()` and `reset()`); per-block `any_needs_if` scan reuses the existing `slot_types_snap` (no extra lock); per-hop populate inside the StftHelper closure reuses the existing `scratch_curr_phase` PLPV buffer (no per-hop alloc). Roll-forward of `if_prev_phase` after each hop. Cost gated to ~1 wrap + 4 FLOPs/bin/hop/channel × `any_needs_if`; zero-cost when no slot opts in. Tests: `tests/instantaneous_freq.rs` (kernel pure-tone resolution at 1 kHz, DC pin, finite-under-random-phase, ModuleType exhaustive non-IF guard, kernel per-call channel independence) + `tests/instantaneous_freq_pipeline.rs` (stub-module integration via `IfProbe` reads `ctx.instantaneous_freq` through `FxMatrix::process_hop`, both Some and None paths). 5b2 prior infra (`if_offset` cache + `PhaseRotator`) is unrelated and predates this Phase. Unblocks Phase 6.5 Harmony, Phase 6.4 Cepstrum, Phase 6.6 FM/Arp NoteIn. |
| 2026-04-27-phase-5b2-past.md | IMPLEMENTED | All 14 tasks merged: first read-only consumer of the History Buffer. `PastModule` with 5 sub-effect modes (Granular Window selective time-windowed freeze of stable bins, DecaySorter temporal reconstruction via summary-stat sorting with 3 SortKey variants Decay/Stability/Area, Spectral Convolution per-bin self-resonance via current⊗past, Reverse backward read with cumulative `reverse_read_offset: u32`, Stretch phase-coherent variable-rate playback 0.25×–4× via `stretch_read_phase: f64` + xorshift32 hash dither). 5 curves (AMOUNT, TIME, THRESHOLD, SPREAD, MIX). Per-channel state: `reverse_read_offset`, `stretch_read_phase`, `sort_order: Vec<u32>` + `sort_scratch: Vec<Complex<f32>>` (MAX_SORT_BINS=256), `stretch_rng: u32` seeded `0xA5A5_A5A5`. Mode change clears per-channel state via `clear_channel_state()` so cross-mode artefacts don't leak. Shared `PhaseRotator` (1024-entry sin/cos LUT, `src/dsp/phase.rs`) and `if_offset[k]` cache (filled per-hop in `Pipeline::process` after analysis FFT, exposed on `ModuleContext`) ship with this phase as Stretch is the first consumer; Future and Punch will reuse them. Per-slot persistence via `Arc<Mutex<[PastMode; 9]>>` + `Arc<Mutex<[SortKey; 9]>>` (matches `slot_life_mode` family); FxMatrix dispatch via `set_past_modes` + `set_past_sort_keys` (both `try_lock`-once). Dedicated `editor/past_popup.rs` (egui temp-data pattern, mirrors `life_popup.rs`) — UX deviation: stays open after mode-click so user can also pick the SortKey for DecaySorter in one trip; only Close button dismisses; popup re-reads `mode_now` after the mode loop to avoid one-frame flicker on DecaySorter transitions. Calibration uses shared `ProbeSnapshot` extended with 5 Past fields (`past_amount_pct` clamped to [0..1] before *100 mirroring module convention, `past_time_seconds` = TIME × `capacity_frames × hop_size`, `past_active_mode_idx`, `past_history_frames_used`, `past_sort_key_idx`) populated as a single struct-literal after the history early-return; `last_probe()` is a one-line override. Pipeline integration smoke test in `tests/past_pipeline.rs` (200 hops × 5 modes × 3 sort keys = 15 combos × 2 channels × 1025 bins, all bins seeded with low-level rotating noise so threshold=0.0 exercises every kernel body, asserts finite + norm < 100). Calibration test in `tests/calibration.rs::past_probe_reports_active_mode_after_dispatch` (Reverse mode end-to-end via real HistoryBuffer); test gated behind `[[test]] required-features = ["probe"]`, matching the `calibration_roundtrip` / `bin_physics_pipeline` pattern. |

## Design specs (`docs/superpowers/specs/`)

| Spec | Status | Notes |
|------|--------|-------|
| 2026-04-12-spectral-forge-design.md | IMPLEMENTED | Foundational; superseded in places by the modular architecture spec. |
| 2026-04-19-modular-architecture-design.md | IMPLEMENTED | Authoritative for slot/matrix/module architecture. |
| 2026-04-21-automation-presets-design.md | IMPLEMENTED | |
| 2026-04-21-bin-physics-infrastructure.md | SUPERSEDED | Replaced by plan `2026-04-27-phase-3-bin-physics.md`. The seven physics-driven module specs below remain DEFERRED pending their full implementations (Phase 5+); the BinPhysics dependency itself is now satisfied. |
| 2026-04-21-circuit-module.md | DEFERRED | Depends on BinPhysics. |
| 2026-04-21-dynamic-automation-naming.md | DEFERRED | Explicitly deferred at design time. |
| 2026-04-21-harmony-module.md | DEFERRED | Depends on BinPhysics + instantaneous-frequency infra. |
| 2026-04-21-kinetics-module.md | DEFERRED | Depends on BinPhysics. |
| 2026-04-21-life-module.md | DEFERRED | Depends on BinPhysics. |
| 2026-04-21-matrix-amp-nodes.md | IMPLEMENTED | Implemented by Phase 2a plan `2026-04-27-phase-2a-matrix-amp-nodes.md`. |
| 2026-04-21-modulate-module.md | DEFERRED | Depends on BinPhysics + instantaneous-frequency infra. |
| 2026-04-21-past-module.md | IMPLEMENTED | Implemented by Phase 5b.2 plan `2026-04-27-phase-5b2-past.md`. The shipped module diverges from this spec in places (5 modes, single-button popup UX); read the plan + code, not this spec, for current behaviour. |
| 2026-04-21-rhythm-module.md | DEFERRED | Depends on BinPhysics + host-BPM-sync infra. |
| 2026-04-21-sidechain-refactor-design.md | IMPLEMENTED | |
| 2026-04-23-ui-parameter-spec-design.md | IMPLEMENTED | **Authoritative source of truth for UI parameter display.** Includes addenda §2.3 / §3.4 / §4.4 merged from calibration-audit T10. |
| 2026-04-24-calibration-audit-design.md | IMPLEMENTED | Paired with plan `2026-04-24-calibration-audit.md`; all 11 tasks merged. |

---

## Rules for editing this file

- When a plan merges to master, flip its row to **IMPLEMENTED** and update the
  matching banner at the top of the plan file.
- When a plan is abandoned, mark it **SUPERSEDED** and name the replacement.
- Never delete a plan or spec — add a status line and leave the content in place
  so later agents can read the history.
- Keep the table entries to one line; put longer context in the individual
  plan/spec banner.
